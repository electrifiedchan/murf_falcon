# Day 1: Setup and Initialization

## Objectives Completed
- **Repository Setup**: Cloned the starter repository and set up the local environment.
- **Dependency Installation**: Installed all necessary dependencies using `uv` for the Python backend and `pnpm` for the Next.js frontend.
- **Environment Configuration**: Configured API keys for LiveKit, Murf (TTS), Deepgram (STT), and Gemini (LLM) in `.env.local` files.
- **Model Downloads**: Successfully downloaded the initial Silero VAD models required for the LiveKit Turn Detector.
- **Local Testing**: Verified that the backend and frontend start correctly via `./start_app.ps1` and that default inference works as expected.
